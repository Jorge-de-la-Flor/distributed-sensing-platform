[project]
name = "edge-node"
version = "0.1.0"
description = "MQTT subscriber, SQLite data store, and REST API for the distributed sensing platform"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "paho-mqtt>=2.1.0",
    "flask>=3.1.0",
]